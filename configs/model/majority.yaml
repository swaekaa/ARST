# ============================================================
# Model Config — Majority / Random Baseline
# ============================================================

name: majority
num_classes: 4
majority_class: 0   # will be overridden at runtime by fit()
